# Archived: original moved to templates/app_original.py
import streamlit as st
st.warning("This file was archived and moved to `templates/app_original.py`.")
st.write("Use `app.py` in the repository root as the app entrypoint (it contains the polished researcher profile and demo).")